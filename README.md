# AudioAZ Showfile Online

채널시트(`.numbers`, `.xlsx`)를 업로드해 Yamaha DM7, KLANG, Waves SuperRack Performer 쇼파일을 생성하는 웹앱입니다.

## Deploy

Render Blueprint에서 `render.yaml`을 사용하거나 Dockerfile로 Web Service를 배포합니다.
